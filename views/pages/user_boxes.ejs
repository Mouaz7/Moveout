<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User's Boxes</title>
</head>
<body>
    <h1>Your Boxes</h1>
    <ul>
        <% userBoxes.forEach(box => { %>
            <li>
                <strong>Box Name:</strong> <%= box.box_name %> <br>
                <strong>Label Design:</strong> <%= box.label_design %> <br>
                <a href="/boxes/view/<%= box.box_id %>">View Box</a> | 
                <a href="/boxes/edit/<%= box.box_id %>">Edit Box</a> | 
                <form action="/boxes/delete/<%= box.box_id %>" method="POST" style="display: inline;">
                    <button type="submit" onclick="return confirm('Are you sure you want to delete this box?')">Delete</button>
                </form>
            </li>
        <% }) %>
    </ul>
</body>
</html>
